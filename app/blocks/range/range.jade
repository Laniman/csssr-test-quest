mixin range(obj)
	+b.range&attributes(attributes)
		+e('input').input(type='range' min=obj.min max=obj.max step=obj.step value=obj.currentValue)
		+e.ruler
			each elem in obj.values
				+e.element(data-value=elem.value)=elem.text
